# Notebook-side dependencies only. The training job installs its own, much larger set
# from scripts/requirements.txt inside the container - nothing here is used for training.
matplotlib>=3.10.3
numpy>=2.0
pandas>=2.2
pyarrow>=15.0
sagemaker>=3.16.0
tqdm==4.66.5
# Notebook 1 measures real token lengths with the base model's tokenizer, which is what
# decides `max_length` in notebook 2. Tokenizer only - no model weights are downloaded.
transformers>=5.13.1
